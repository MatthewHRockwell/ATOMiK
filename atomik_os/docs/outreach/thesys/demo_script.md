# 2-min demo script — Thesys

Same flow as the v0 demo with one substitution at 1:10:

**1:10 — schema interop.** "We already render AdaptiveCards verbatim
through `adapters/adaptivecards_to_atomik.py`. A Thesys adapter is
shaped the same way — your output JSON in, our delta-log wire format
out. The renderer doesn't care about the source schema."

**1:30 — the ask, Thesys-flavored.** "Thesys generates UI; we put it
on hardware that can't run a browser. I'd love a 20-minute walk-
through of your output format so I can ship a Thesys adapter on
day one."

Same close: "Code is open, hardware is here, I can demo live tomorrow."
