# Design-partner outreach

Closes task #41. Four target partners, each with a cold email, a demo
script, and a one-page brief. Send when convenient — none are blockers.

The plan if nobody responds (likely; cold outreach to large companies
from a solo founder rarely lands fast): keep shipping. AdaptiveCards
is already a working integration, so we have at least one schema-
interop story without a single conversation.

| Target | Who to email | Status | Folder |
|--------|--------------|--------|--------|
| Vercel v0 | community@vercel.com / DM @rauchg | not sent | `vercel_v0/` |
| Thesys | hello@thesys.dev | not sent | `thesys/` |
| Microsoft AdaptiveCards | adaptivecards@microsoft.com | not sent | `microsoft_adaptivecards/` |
| Anthropic Artifacts | (no public address; via existing relationships) | not sent | `anthropic_artifacts/` |

**Default move while waiting:** advance the `atomik_os` roadmap as if no
response is coming. AdaptiveCards adapters already work; that's our
schema-interop proof on its own.

## Common asset: the verified screenshot

`docs/screenshots/deploy_20260504_162651_v0.18.png` is the evidence
attached to every outreach. 1080p, captured from the AX7020 HDMI
output, shows the OS running with version banner.
